spring:
  datasource:
    url: jdbc:oracle:thin:@localhost:1521:xe
    username: ${ORACLE_USERNAME:edi}
    password: ${ORACLE_PASSWORD:1234}
    driver-class-name: oracle.jdbc.OracleDriver
    hikari:
      maximum-pool-size: 5
      minimum-idle: 2

logging:
  level:
    com.seniorcare: DEBUG
    org.springframework.security: DEBUG
    org.mybatis: DEBUG
